Zijn alle endpoints correct en nodig?

Flows?


User plaatst order 

ShippingOrder -> /api/order/{orderId}/ship
Endpoint puur voor handeling, Dit wordt een POST --> Azure function? 

UserInfo is shared to another department. GetUserById. 

Review on a product -> POST /api/product/{productId}/review --> Could have?


2 azure functions. Order plaatsen != shippen. Timetrigger. Alle orders ophalen en kijken of ze verstuurd.
Elke order die geen shippingdate heeft, roept andere azurefunctie aan --> HttpTrigger --> Post Order/{OrderId}/ship



TODO List: 

- orderservice aanmaken
- orderservice toevoegen aan program.cs
- in orderservice -> functie aanroepen vanuit de controller
- logica controller --> naar die functie
- Models map met order
- content Http call = json --> mappen naar Order
- Lijst met orders, http call naar de nieuwe endpoint (httpclient post -> api/orders/{orderid}/ship)
foreach orderService